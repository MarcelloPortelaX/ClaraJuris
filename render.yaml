services:
  - type: web
    name: clarajuris-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
